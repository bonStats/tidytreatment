# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
r:
  - oldrel
  - release
  - devel
os:
  - linux
  - osx
arch:
  - x64
  - x86
jobs:
  exclude:
    - r: oldrel
      os: osx
cache: packages
osx_image: xcode12.5
