% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy-posterior-stochtree.R
\name{linpred_draws.bartmodel}
\alias{linpred_draws.bartmodel}
\title{Get linear predictor draws from posterior of \code{bartmodel} (\code{stochtree} package) model}
\usage{
\method{linpred_draws}{bartmodel}(
  object,
  newdata,
  rfx_group_ids = NULL,
  rfx_basis = NULL,
  value = ".linpred",
  ...,
  ndraws = NULL,
  include_newdata = TRUE
)
}
\arguments{
\item{object}{A \code{bartmodel} model from the \code{stochtree} package.}

\item{newdata}{Data frame to generate fitted values from. If omitted, defaults to the data used to fit the model.}

\item{rfx_group_ids}{Random effect group labels for \code{newdata} (required if the model was fit with random effects and \code{newdata} is supplied).}

\item{rfx_basis}{Random effect basis for \code{newdata} (required if the model was fit with a \code{"custom"} random effects basis and \code{newdata} is supplied; optional/ignored otherwise).}

\item{value}{The name of the output column for \code{linpred_draws}; default \code{".linpred"}.}

\item{...}{Additional arguments passed to \code{predict.bartmodel}.}

\item{ndraws}{Not currently implemented.}

\item{include_newdata}{Should the newdata be included in the tibble?}
}
\value{
A tidy data frame (tibble) with linear predictor values.
}
\description{
Typically referred to as fitted value draws on the linear (pre-link) scale. For a continuous
outcome model (identity link) this is identical to \code{epred_draws}; for a binary outcome
model it is the latent/probit-scale value, before the link function is applied.
}
